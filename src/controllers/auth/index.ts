export * from "./authenticate";

// Базовый пример контроллера аутентификации
export const login = async (req: any, res: any) => {
  // Логика входа
};

export const register = async (req: any, res: any) => {
  // Логика регистрации
};
